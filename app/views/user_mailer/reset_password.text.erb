<% if Rails.env.development? %>
  <%= "http://#{ENV["LOCALHOST"]}/password_resets/#{@user.reset_password_token}" %>
<% elsif Rails.env.production? %>
  <%= "https://#{ENV["SERVICE_DOMAIN"]}/password_resets/#{@user.reset_password_token}" %>
<% end %>
